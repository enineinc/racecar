# Generated: Authorization Server vhost (auth.*), :__PORT__ (settings.auth).
<VirtualHost *:443>
    ServerName auth.example.com
    ProxyPreserveHost On
    ProxyPass        / http://127.0.0.1:__PORT__/
    ProxyPassReverse / http://127.0.0.1:__PORT__/
</VirtualHost>
