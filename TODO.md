---
pnode: [README.md]
---

# TODO

The index. Open work lives with its concern; this resolver routes to each (TODO_FORMAT.md source schema). It does not restate the items.

## TODO
- Wall rebuild (the craft refactor) - [PLAN.md](PLAN.md#todo) · flight plan [PLAN.md](PLAN.md#plan)

## Completed
- 2026-07-18 — SG3: make the remaining package-only canon shape-aware — the `django` config-home is exempt from the `standalone-pylintrc` Blocker and from the package-only required pre-commit hooks (`import-linter`, `validate-pyproject`, `no-upward-imports`). Completes SG2's config-home reasoning (`check_forbidden_pylintrc` / `check_precommit`). See [arch-coherence/PACKAGING.md](arch-coherence/PACKAGING.md) §"Scope".
- 2026-07-18 — SG2: recognize the flat Django shape (root `manage.py`, no library — the `django-admin startproject` canon) as a first-class `django` shape in `detect_shape` + `racecar.mk`; its root pyproject is treated as a config-home (library audit skipped). Escalated by a real adopter (django > racecar). See [arch-coherence/PACKAGING.md](arch-coherence/PACKAGING.md) §"Scope".
- 2026-06-23 — Surfaces detector precision (FD1): Pattern-1 discovery-root suppression in `check_surface_orchestration` (SURFACES.md §7)
