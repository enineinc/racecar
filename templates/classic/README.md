# <project_name>

<!-- Optional, conventional for shared projects: badges (build, version, coverage)
     go right here under the title, and a short table of contents below the value
     proposition for a long README. Delete this comment; add them only if useful. -->

<!-- Value proposition (one paragraph, no heading). Who is this for, and what does
     it do for them? Lead with the benefit, not the implementation. -->
<project_name> is <who it is for> that <what it does for them>.

## What it does for you

<!-- The benefit in the reader's terms: what problem does it remove, what do they
     get? A paragraph or two. This is "what", framed as outcome, not a feature list. -->
<the problem it removes / what the reader gets>

## Getting Started

<!-- The shortest path to a working state: install, then confirm it runs. Keep it to
     the two or three commands a newcomer actually types first. -->
1. **Install:**

       make install-dev

2. **Confirm it works:**

       make check

## Using <project_name>

<!-- The recurring workflows: how a reader interacts with this day to day. The
     subsections below are a MENU, not a checklist. Keep the ones that apply, delete
     the rest. None are mandatory. -->

**Commands.** <the main verbs / entry points and what each does>

**How it's structured.** <the shape a contributor must hold — e.g. lib -> api -> faces:
the library does the work, api owns orchestration, each face is a thin wrapper on api.
Delete if the project has no structure worth stating up front.>

**Example.** <one concrete end-to-end use: input -> what happens -> output>

## When, where, and why

<!-- OPTIONAL section. Delete it if there is nothing non-obvious to say. This is the
     rationale a reader wants only after they are bought in: why key choices were made,
     where it runs, when to reach for it. Never write this section for its own sake. -->
<design rationale, constraints, or deployment context — only if non-obvious>

## Contributing

<!-- Expected for any shared or open-source project; delete for a private solo repo.
     How a contributor sets up and what you expect of a change. -->
Set up with `make install-dev`, then run `make check` before opening a pull request.
<add project-specific expectations, or link a CONTRIBUTING.md if you have one>

## License

<!-- Name the license; link the LICENSE file once it exists in your repo. -->
<license name>. See the `LICENSE` file.
